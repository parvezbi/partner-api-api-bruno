info:
  name: sendResetOtp
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/partnerapp/send/reset/otp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
