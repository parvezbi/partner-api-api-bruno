info:
  name: login
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/partnerapp/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
