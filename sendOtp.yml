info:
  name: sendOtp
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/partnerapp/send/otp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
