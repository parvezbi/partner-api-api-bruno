info:
  name: verifyOtp
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/partnerapp/verify/otp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
